# Day 19: Security Architecture

## Status: ⬜ Not Started

---

## 📚 Learning Goals

### 1. OAuth2
- [ ] Understand OAuth2 flows
- [ ] Know when to use each

```
┌─────────────────────────────────────────────────────────────────────────┐
│  OAuth2 Authorization Code Flow (with PKCE)                              │
│                                                                         │
│   User          Client App         Auth Server         Resource Server  │
│    │                │                    │                    │         │
│    │ 1. Login      │                    │                    │         │
│    │──────────────►│                    │                    │         │
│    │               │                    │                    │         │
│    │               │ 2. Redirect to     │                    │         │
│    │◄──────────────│    Auth Server     │                    │         │
│    │               │                    │                    │         │
│    │ 3. Login + Consent                 │                    │         │
│    │───────────────────────────────────►│                    │         │
│    │               │                    │                    │         │
│    │ 4. Auth Code  │                    │                    │         │
│    │◄──────────────────────────────────│                    │         │
│    │               │                    │                    │         │
│    │ 5. Auth Code  │                    │                    │         │
│    │──────────────►│                    │                    │         │
│    │               │                    │                    │         │
│    │               │ 6. Exchange Code   │                    │         │
│    │               │    + PKCE verifier │                    │         │
│    │               │───────────────────►│                    │         │
│    │               │                    │                    │         │
│    │               │ 7. Access Token    │                    │         │
│    │               │◄───────────────────│                    │         │
│    │               │                    │                    │         │
│    │               │ 8. API Request     │                    │         │
│    │               │    + Access Token  │                    │         │
│    │               │──────────────────────────────────────►│         │
│    │               │                    │                    │         │
│    │               │ 9. Protected Data  │                    │         │
│    │               │◄──────────────────────────────────────│         │
│    │               │                    │                    │         │
└─────────────────────────────────────────────────────────────────────────┘
```

**OAuth2 Flows:**

| Flow | Use Case | Security Level |
|------|----------|----------------|
| **Authorization Code + PKCE** | Web apps, mobile | Highest |
| **Client Credentials** | Machine-to-machine | High |
| **Device Code** | Smart TVs, CLI | Medium |
| **Implicit** (deprecated) | Legacy SPAs | Low |

---

### 2. JWT (JSON Web Tokens)
- [ ] Understand JWT structure
- [ ] Know security considerations

```
┌─────────────────────────────────────────────────────────────────────────┐
│  JWT Structure                                                           │
│                                                                         │
│  Header.Payload.Signature                                               │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                          HEADER                                    │ │
│  │  {                                                                │ │
│  │    "alg": "RS256",                                               │ │
│  │    "typ": "JWT"                                                  │ │
│  │  }                                                                │ │
│  │  ──► Base64Url encoded                                           │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              .                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                          PAYLOAD                                   │ │
│  │  {                                                                │ │
│  │    "sub": "user123",                                             │ │
│  │    "name": "John Doe",                                           │ │
│  │    "iat": 1516239022,                                            │ │
│  │    "exp": 1516242622,                                            │ │
│  │    "roles": ["admin", "user"]                                    │ │
│  │  }                                                                │ │
│  │  ──► Base64Url encoded                                           │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              .                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                         SIGNATURE                                  │ │
│  │  RSASHA256(                                                       │ │
│  │    base64UrlEncode(header) + "." + base64UrlEncode(payload),     │ │
│  │    privateKey                                                     │ │
│  │  )                                                                │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  JWT Best Practices:                                                   │
│  • Use RS256 (asymmetric) over HS256 (symmetric)                      │
│  • Keep tokens short-lived (15 min access, 7 day refresh)            │
│  • Store in httpOnly cookies or memory (not localStorage)            │
│  • Validate all claims (exp, iss, aud)                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 3. RBAC (Role-Based Access Control)
- [ ] Understand RBAC model
- [ ] Know permission design

```
┌─────────────────────────────────────────────────────────────────────────┐
│  RBAC Model                                                              │
│                                                                         │
│   Users                 Roles               Permissions                 │
│  ┌───────┐           ┌─────────┐          ┌─────────────────┐          │
│  │ Alice │──────────►│  Admin  │─────────►│ users:read      │          │
│  └───────┘           │         │          │ users:write     │          │
│                      │         │          │ users:delete    │          │
│  ┌───────┐           │         │          │ orders:read     │          │
│  │  Bob  │──────────►│  Admin  │          │ orders:write    │          │
│  └───────┘           └─────────┘          │ settings:manage │          │
│                                           └─────────────────┘          │
│  ┌───────┐           ┌─────────┐          ┌─────────────────┐          │
│  │ Carol │──────────►│  Editor │─────────►│ orders:read     │          │
│  └───────┘           │         │          │ orders:write    │          │
│                      └─────────┘          └─────────────────┘          │
│                                                                         │
│  ┌───────┐           ┌─────────┐          ┌─────────────────┐          │
│  │  Dave │──────────►│  Viewer │─────────►│ orders:read     │          │
│  └───────┘           │         │          └─────────────────┘          │
│                      └─────────┘                                       │
│                                                                         │
│  Permission Check: user.roles.some(r => r.permissions.includes(perm)) │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**RBAC Implementation:**

```typescript
// Permission decorator
@Controller('users')
export class UsersController {
  
  @Get()
  @RequirePermissions('users:read')
  findAll() {
    return this.usersService.findAll();
  }
  
  @Delete(':id')
  @RequirePermissions('users:delete')
  remove(@Param('id') id: string) {
    return this.usersService.remove(id);
  }
}

// Guard
@Injectable()
export class PermissionsGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const requiredPermissions = this.reflector.get<string[]>(
      'permissions',
      context.getHandler()
    );
    
    const user = context.switchToHttp().getRequest().user;
    
    return requiredPermissions.every(perm => 
      user.permissions.includes(perm)
    );
  }
}
```

---

### 4. Zero Trust
- [ ] Understand Zero Trust principles
- [ ] Know implementation strategies

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Zero Trust Architecture                                                 │
│                                                                         │
│  Traditional (Castle and Moat):                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  Firewall                                                        │  │
│  │  ┌──────────────────────────────────────────────────────────┐   │  │
│  │  │  Internal Network (Trusted)                              │   │  │
│  │  │                                                          │   │  │
│  │  │  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐            │   │  │
│  │  │  │Users  │──│Service│──│Service│──│  DB   │            │   │  │
│  │  │  └───────┘  └───────┘  └───────┘  └───────┘            │   │  │
│  │  │         ▲                                               │   │  │
│  │  │         └── Once inside, everything is trusted ⚠️      │   │  │
│  │  └──────────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  Zero Trust (Never Trust, Always Verify):                              │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  ┌───────┐    ┌─────────┐    ┌───────┐    ┌─────────┐          │  │
│  │  │ User  │───►│Identity │───►│ Policy│───►│ Service │          │  │
│  │  │       │    │Provider │    │ Engine│    │         │          │  │
│  │  └───────┘    └─────────┘    └───┬───┘    └────┬────┘          │  │
│  │       │                          │             │                │  │
│  │       │      Every request:      │             │                │  │
│  │       │      • Authenticated     │             │                │  │
│  │       │      • Authorized        │             │                │  │
│  │       │      • Encrypted         │             │                │  │
│  │       │      • Logged            │             │                │  │
│  │       │                          │             ▼                │  │
│  │       │                    ┌─────┴─────┐  ┌───────┐            │  │
│  │       │                    │  Service  │──│  DB   │            │  │
│  │       │                    │  (mTLS)   │  │(Encr.)│            │  │
│  │       │                    └───────────┘  └───────┘            │  │
│  │       │                                                        │  │
│  └───────┴────────────────────────────────────────────────────────┘  │
│                                                                         │
│  Zero Trust Principles:                                                │
│  1. Verify explicitly (always authenticate and authorize)             │
│  2. Use least privilege access                                        │
│  3. Assume breach (minimize blast radius)                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 5. OWASP Top 10
- [ ] Know common vulnerabilities
- [ ] Understand mitigations

```
┌─────────────────────────────────────────────────────────────────────────┐
│  OWASP Top 10 (2021)                                                     │
│                                                                         │
│  1. Broken Access Control                                              │
│     └── Mitigation: Implement proper authorization checks              │
│                                                                         │
│  2. Cryptographic Failures                                             │
│     └── Mitigation: Use strong encryption, proper key management       │
│                                                                         │
│  3. Injection (SQL, NoSQL, Command)                                    │
│     └── Mitigation: Parameterized queries, input validation            │
│                                                                         │
│  4. Insecure Design                                                    │
│     └── Mitigation: Threat modeling, secure design patterns            │
│                                                                         │
│  5. Security Misconfiguration                                          │
│     └── Mitigation: Hardening, automated configs, security headers     │
│                                                                         │
│  6. Vulnerable Components                                              │
│     └── Mitigation: Dependency scanning, regular updates               │
│                                                                         │
│  7. Authentication Failures                                            │
│     └── Mitigation: MFA, rate limiting, secure password policies       │
│                                                                         │
│  8. Software and Data Integrity Failures                               │
│     └── Mitigation: Code signing, integrity checks, secure CI/CD       │
│                                                                         │
│  9. Security Logging and Monitoring Failures                           │
│     └── Mitigation: Centralized logging, alerting, audit trails        │
│                                                                         │
│  10. Server-Side Request Forgery (SSRF)                                │
│      └── Mitigation: URL validation, network segmentation              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📘 Advanced Security Topics

### PKI (Public Key Infrastructure)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  PKI Hierarchy                                                           │
│                                                                         │
│                    ┌─────────────────┐                                  │
│                    │    Root CA      │                                  │
│                    │   (Offline)     │                                  │
│                    └────────┬────────┘                                  │
│                             │                                           │
│              ┌──────────────┼──────────────┐                           │
│              │              │              │                           │
│              ▼              ▼              ▼                           │
│       ┌──────────┐   ┌──────────┐   ┌──────────┐                      │
│       │Issuing CA│   │Issuing CA│   │Issuing CA│                      │
│       │(Internal)│   │(External)│   │(Partners)│                      │
│       └────┬─────┘   └────┬─────┘   └────┬─────┘                      │
│            │              │              │                             │
│            ▼              ▼              ▼                             │
│       ┌─────────┐    ┌─────────┐    ┌─────────┐                       │
│       │End Certs│    │End Certs│    │End Certs│                       │
│       │(mTLS)   │    │(TLS)    │    │(VPN)    │                       │
│       └─────────┘    └─────────┘    └─────────┘                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Encryption

```
At Rest:                          In Transit:
┌──────────────────────┐         ┌──────────────────────┐
│  Database            │         │  Client → Server     │
│  ┌────────────────┐  │         │                      │
│  │ Encrypted Data │  │         │  TLS 1.3             │
│  │ (AES-256-GCM)  │  │         │  • Certificate       │
│  └────────────────┘  │         │  • Key Exchange      │
│                      │         │  • Symmetric Encrypt │
│  Key: AWS KMS        │         │                      │
└──────────────────────┘         └──────────────────────┘
```

### Secrets Management

```typescript
// Using AWS Secrets Manager
const secretsManager = new AWS.SecretsManager();

async function getDatabaseCredentials() {
  const secret = await secretsManager.getSecretValue({
    SecretId: 'prod/database/credentials'
  }).promise();
  
  return JSON.parse(secret.SecretString);
}

// Never in code:
// ❌ const DB_PASSWORD = 'hardcoded-secret';

// Always from secrets manager:
// ✓ const { password } = await getDatabaseCredentials();
```

---

## 🎯 Practice Task

### Design Authentication System

**Instructions:**
1. Design OAuth2 + JWT authentication
2. Implement RBAC
3. Add security headers
4. Plan secrets management

---

## 📝 Notes

*Add your notes here during learning*

---

## 📖 Resources

- [ ] [OWASP](https://owasp.org/)
- [ ] [OAuth 2.0 Specification](https://oauth.net/2/)
- [ ] [JWT.io](https://jwt.io/)
- [ ] [Zero Trust Architecture](https://www.nist.gov/publications/zero-trust-architecture)

---

## ✅ Completion Checklist

- [ ] Understand OAuth2 flows
- [ ] Know JWT structure and security
- [ ] Can design RBAC system
- [ ] Understand Zero Trust
- [ ] Know OWASP Top 10

**Date Completed:** _____________
